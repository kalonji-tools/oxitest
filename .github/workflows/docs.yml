name: Docs

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  changes:
    name: Detect docs changes
    runs-on: ubuntu-latest
    outputs:
      docs: ${{ steps.filter.outputs.docs }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            docs:
              - 'docs/src/**'
              - 'mkdocs.yml'
              - 'pyproject.toml'

  build:
    name: Build docs
    needs: changes
    if: needs.changes.outputs.docs == 'true' || github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # required by git-revision-date-localized

      - uses: astral-sh/setup-uv@v7
        with:
          python-version: '3.12'

      - run: uv sync --only-group docs

      - name: Build
        run: uv run mkdocs build --strict

      - uses: actions/upload-pages-artifact@v3
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
        with:
          path: docs/site

  required:
    name: Build docs (required)
    needs: [build]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check build result
        shell: bash
        run: |
          if [[ "${{ needs.build.result }}" == "failure" ]]; then
            echo "Docs build failed"
            exit 1
          fi
          echo "Docs build passed or was skipped"

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
