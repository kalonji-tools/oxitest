name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            code:
              - 'src/**'
              - 'python/**'
              - 'Cargo.*'
              - 'rust-toolchain.toml'
              - 'pyproject.toml'
              - '.github/**'

  rust-tests:
    name: Rust Tests
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      # Toolchain comes from rust-toolchain.toml — see that file (#1792).
      - run: rustup toolchain install

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test

      - uses: ./.github/actions/setup-python
        with:
          uv-sync-args: --group test

      - uses: taiki-e/install-action@v2.85.10
        with:
          tool: cargo-llvm-cov,cargo-insta

      - name: Check for unreferenced snapshots
        run: cargo insta test --unreferenced=reject

      - name: Rust tests with coverage
        run: cargo llvm-cov --cobertura --output-path cobertura.xml

      - name: Upload Rust coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          files: cobertura.xml
          flags: rust
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

  python-tests:
    name: Test (Python ${{ matrix.python-version }})
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11', '3.12', '3.13', '3.14']
    steps:
      - uses: actions/checkout@v7

      # Toolchain comes from rust-toolchain.toml — see that file (#1792).
      - run: rustup toolchain install

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test
          save-if: ${{ matrix.python-version == '3.12' }}

      - uses: ./.github/actions/setup-python
        with:
          python-version: ${{ matrix.python-version }}
          uv-sync-args: --group test

      - name: Build extension
        run: uv run maturin develop

      - name: Python tests
        if: matrix.python-version != '3.12'
        run: uv run python -m oxitest

      - name: Python tests (with coverage)
        if: matrix.python-version == '3.12'
        run: uv run python -m oxitest --cov --cov-report xml

      - name: Upload extension artifact
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@v7
        with:
          name: extension-3.12
          path: python/oxitest/_oxitest.cpython-312-*.so
          retention-days: 1

      - name: Upload Python coverage to Codecov
        if: matrix.python-version == '3.12'
        uses: codecov/codecov-action@v7
        with:
          files: coverage.xml
          flags: python
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

  # Three discrete jobs rather than one matrix. A matrix reports a single
  # aggregate `needs.<job>.result` — see how `python-tests` above, four legs,
  # reaches the rollup below as one string — so per-platform promotion into
  # `needs:` would be impossible. The duplication buys that granularity, and
  # collapsing these back into a matrix silently reverts it (#1944).
  #
  # These cover the *platform* each wheel targets, and not the wheel: both
  # halves of the macOS `universal2` target and the cross-built Linux `aarch64`
  # target. They run `maturin develop`, so no artifact is involved and no
  # wheel is read. The artifact is covered by `wheel-manifest` below and by
  # `publish.yml`'s gate (#2177). Python 3.12 only — version and platform are
  # orthogonal axes, and four versions are already covered on Linux.
  #
  # Deliberately lighter than the Linux jobs: no `insta --unreferenced`
  # (snapshot hygiene is repo-wide and Linux gates it), no `llvm-cov` or
  # Codecov upload (three more uploads would double-count the same flags),
  # no artifact upload (`extension-3.12` would collide).
  #
  # Their shared steps live in `.github/actions/platform-test` (#1959).
  macos-arm:
    name: Test (macOS arm64)
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/platform-test

  macos-intel:
    name: Test (macOS x86_64)
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: macos-15-intel
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/platform-test

  linux-arm:
    name: Test (Linux arm64)
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/platform-test

  # A configuration variant, not a platform. TMPDIR points at a symlink, which
  # reproduces on Linux the path-spelling class #1957 decodes: mkdtemp() returns
  # the unresolved spelling while Path.cwd() reports the resolved one, so any
  # comparison between them fails. Analogous to macOS rather than identical —
  # there the symlinked component is a *parent* (/var -> /private/var), here it
  # is the leaf. The divergence that reproduces the class is the same (#1964).
  #
  # TMPDIR is exported through $GITHUB_ENV rather than a job-level `env:` block.
  # Job-level env cannot reference the `runner` context at all — actionlint
  # rejects it: "context \"runner\" is not allowed here". $GITHUB_ENV also fixes
  # the ordering problem it would have created, because the variable only applies
  # to steps *after* the one that writes it, so the symlink is guaranteed to
  # exist by the time anything reads TMPDIR. The only constraint left is the
  # natural one: this step precedes the composite.
  #
  # It therefore covers every step the composite runs — rustup, rust-cache,
  # setup-uv, uv sync, cargo test and maturin — not just the suite. Deliberate:
  # `cargo test` under a symlinked root was measured green (1686 passed) before
  # this landed, and it reaches Rust's canonicalize sites nothing else does.
  tmpdir-symlink:
    name: Test (symlinked TMPDIR)
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - name: Point TMPDIR at a symlinked temp root
        run: |
          mkdir -p "$RUNNER_TEMP/private/T"
          ln -s "$RUNNER_TEMP/private/T" "$RUNNER_TEMP/T"
          # Without this the job degrades silently: if the link ever stops
          # diverging, the suite runs under an ordinary path and passes,
          # reporting coverage it does not have. A no-op gate is worse than no
          # gate, so fail loudly instead (#1964).
          [ "$(readlink -f "$RUNNER_TEMP/T")" != "$RUNNER_TEMP/T" ] \
            || { echo "TMPDIR is not symlinked — this gate would be a no-op"; exit 1; }
          echo "TMPDIR=$RUNNER_TEMP/T" >> "$GITHUB_ENV"

      - uses: ./.github/actions/platform-test

  # Required. It landed advisory, against map #1946 standing preference 5, by
  # maintainer decision: #1986 took Windows from 292 non-passes to 39, and
  # holding a shipped gate-backed improvement behind that residue was the worse
  # trade. #1989 then cleared the 39 and promoted the job into the `required`
  # job's `needs:` below, so the exception closed rather than becoming the
  # permanent-advisory state preference 5 warns about.
  #
  # Windows is therefore in the supported set, and ADR-0013 derives its wheel
  # target and its classifier from that (#1950).
  windows:
    name: Test (Windows x86_64)
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: windows-latest
    # 45, against the other platform jobs' 30. #1951 measured the full
    # sequence at ~16 min on this runner against ~6 on Linux arm64, and a
    # Windows-slowness timeout would read as a product failure.
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/platform-test

  # The source-controlled half of the Distribution band (ADR-0019). Properties
  # 1 and 2 — the file manifest, and the import when the source tree is not the
  # working directory — are controlled by the source, so a pull request can
  # break them. Properties 3, 4 and 5 are controlled by the release matrix, so
  # a pull request cannot reach them and this job does not claim to.
  #
  # It calls `scripts/check_artifact.py` with a shorter `--properties` value
  # than `publish.yml`'s gate. The subset is a subset by argument, so it cannot
  # drift into an assertion the release gate does not make.
  #
  # A configuration variant rather than a platform, so it is in
  # `NON_PLATFORM_JOBS` in `scripts/check_platform_sets.py` — it confers no
  # platform support and must not be read as a wheel target.
  wheel-manifest:
    name: Test (wheel manifest)
    needs: changes
    if: needs.changes.outputs.code == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      # Toolchain comes from rust-toolchain.toml — see that file (#1792).
      - run: rustup toolchain install

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: test

      - uses: ./.github/actions/setup-python
        with:
          uv-sync-args: --group build

      # `maturin build`, not `maturin develop`. `develop` writes the extension
      # into the source tree and produces no artifact, which is exactly the
      # state this job exists to stop being the only one tested.
      - name: Build a wheel
        run: uv run maturin build --out dist

      # This job keeps a virtual environment where `publish.yml`'s gate legs
      # dropped theirs. The reason is the interpreter each one starts from.
      # There, `actions/setup-python` gives a clean interpreter and installing
      # into it is safe. Here the interpreter is uv's project environment,
      # which already holds oxitest as an editable install — installing the
      # wheel into it would replace the thing the rest of CI builds, and the
      # import property could then pass against the editable install.
      #
      # `uv run python`, not a bare `python`. This job's interpreter comes from
      # `.github/actions/setup-python`, which is `astral-sh/setup-uv`, and that
      # does not promise a bare `python` on PATH the way `actions/setup-python`
      # does in `publish.yml`. `python -m venv` still creates a standard
      # environment with pip in it, which `uv venv` would not.
      - name: Install the wheel
        run: |
          uv run python -m venv "$RUNNER_TEMP/gate"
          "$RUNNER_TEMP/gate/bin/python" -m pip install --disable-pip-version-check \
            --no-index --only-binary :all: --find-links dist oxitest

      - name: Assert the properties
        working-directory: ${{ runner.temp }}
        run: |
          "$RUNNER_TEMP/gate/bin/python" "$GITHUB_WORKSPACE/scripts/check_artifact.py" \
            --source-root "$GITHUB_WORKSPACE" --properties manifest,import

      - name: Run the CLI once
        working-directory: ${{ runner.temp }}
        run: '"$RUNNER_TEMP/gate/bin/oxitest" --version'

  required:
    name: Tests (required)
    needs: [changes, rust-tests, python-tests, linux-arm, macos-arm, macos-intel, tmpdir-symlink, windows, wheel-manifest]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check the change filter ran
        shell: bash
        run: |
          # `changes` decides what gets tested, so unlike the jobs below it has
          # no `skipped` allowance (#1925). It carries no `if:` and no `needs:`,
          # so it always runs: any result other than success means the test
          # selection is unknown rather than empty (#1961).
          case "${{ needs.changes.result }}" in
            success) ;;
            *) echo "Detect changes ended in '${{ needs.changes.result }}'; the test selection is unknown"; exit 1 ;;
          esac

      - name: Check results
        shell: bash
        env:
          NEEDS: ${{ toJSON(needs) }}
        run: |
          # Allowlist, not a failure-blocklist — see docs.yml's `required` job (#1925).
          #
          # The results are read out of `needs` rather than named one by one
          # (#2072). A rollup used to hold one invariant in two literals — the
          # `needs:` list above and the results a step named — and a job added
          # to the first but not the second was waited on and never checked
          # (#1944). `scripts/check_rollup_agreement.py` existed to compare
          # them. Reading the set from `toJSON(needs)` leaves one literal, so
          # there is no second copy to drift from.
          #
          # `changes` is deleted here, not allowed through: it is checked by the
          # step above on the stricter path, where `skipped` does not pass. That
          # is the defect #1961 fixed and it must not be given a `skipped`
          # allowance here.
          #
          # `length > 0` is the vacuity guard. `all` is true over an empty set,
          # so without it a rollup that waits on nothing reports success while
          # guarding nothing.
          #
          # The observed set is printed before it is judged, on every run. That
          # is the only record of what `toJSON(needs)` actually serialises.
          #
          # Whether a skipped job appears here at all decides whether the
          # vacuity guard can fire spuriously. It does appear, carrying
          # `result: "skipped"` — measured 2026-08-12 on run 31608777438, where
          # `Docs (required)` printed `build: skipped` / `doc-tests: skipped`
          # and passed. So `length` counts skipped jobs, `length == 0` is
          # unreachable by jobs skipping, and the guard is reached only by
          # editing the `needs:` list above (#2076).
          # Read this list against the `needs:` above when changing either.
          #
          # This block is deliberately NOT extracted into a composite action,
          # and the three copies are deliberate (#2075). actionlint cannot read
          # a composite — it parses one as a workflow and reports spurious
          # syntax errors (#1974) — so moving these lines out would cost them
          # their shellcheck coverage, to save two edits. The drift that three
          # copies risk is held by `python/tests/test_rollup_results_check.py`,
          # which reads this program out of every rollup and asserts they agree.
          #
          # Do not start a comment line here with the word shellcheck: it is
          # read as a `# shellcheck …` directive and fails the lint (SC1073).
          echo "Jobs this rollup waited on:"
          echo "$NEEDS" | jq -r 'del(.changes) | to_entries[] | "  \(.key): \(.value.result)"'
          echo "$NEEDS" | jq -e '
            del(.changes)
            | (length > 0)
              and all(.[]; .result == "success" or .result == "skipped")
          ' > /dev/null || {
            echo "A test job did not pass; only success or skipped may pass"
            exit 1
          }
          echo "All jobs passed or were skipped"
