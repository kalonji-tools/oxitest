[tool.oxitest]
testpaths = ["package_boundary_nested"]
python_files = ["test_*.py"]
strict = "abort"
# Same reason as package_boundary_order: arrangement would move these modules
# into a different phase, and phases use different fixture sessions.
auto_arrange = false
