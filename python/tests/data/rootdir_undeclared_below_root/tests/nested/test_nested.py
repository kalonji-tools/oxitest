"""One test, purely to make ``nested/`` a directory collection will visit."""

from __future__ import annotations


def test_placeholder() -> None:
    assert True, "the run must fail at collection, before this test executes"
